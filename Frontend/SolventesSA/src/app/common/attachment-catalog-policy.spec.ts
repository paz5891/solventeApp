import { AttachmentCatalogPolicy } from './attachment-catalog-policy';

describe('AttachmentCatalogPolicy', () => {
  it('should create an instance', () => {
    expect(new AttachmentCatalogPolicy()).toBeTruthy();
  });
});
